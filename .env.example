# 数据层。题库、卷子、判卷、成绩、以及「这枚 cookie 是谁」全都问它 —— 这个站
# 点不碰数据库，也没有自己的会话格式。
CAN_API_ORIGIN=https://api.ceruleanavi.net

# 主站。页眉那排导航和**唯一的登录入口**都在那边；这里是另一个源，写相对路径
# 会打在 exam.ceruleanavi.net 上然后 404。
#
# 本地开发时主站在 :4321，不改这个值的话开发机上点「登录」会跳到线上去。
CAN_WEB_ORIGIN=https://ceruleanavi.net

# 这个部署自己的对外地址，用来校验写操作的 Origin。
#
# **部署里必须设。** 不设就是下面这个开发机地址，而浏览器发来的 Origin 是线上
# 域名，两者对不上 —— 交卷会稳定地 403，而那是这个站点最不能坏的一个动作。
PUBLIC_ORIGIN=http://localhost:4324
